image:
  pullPolicy: IfNotPresent
  repository: khlieng/dispatch
  tag: latest
persistence:
  data:
    enabled: true
    mountPath: /data
portal:
  open:
    enabled: true
securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsGroup: 0
    runAsUser: 0
service:
  https:
    enabled: true
    ports:
      https:
        enabled: true
        port: 4596
        protocol: tcp
        targetPort: 443
  main:
    ports:
      main:
        port: 8000
        protocol: http
        targetPort: 80
