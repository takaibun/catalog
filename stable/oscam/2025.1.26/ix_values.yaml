image:
  pullPolicy: IfNotPresent
  repository: ghcr.io/linuxserver/oscam
  tag: latest
persistence:
  config:
    enabled: true
    mountPath: /config
  varrun:
    enabled: true
portal:
  open:
    enabled: true
securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsGroup: 20
    runAsNonRoot: false
    runAsUser: 0
service:
  main:
    ports:
      main:
        port: 10062
        targetPort: 8888
